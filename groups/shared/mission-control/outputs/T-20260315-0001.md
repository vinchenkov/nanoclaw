# Task: T-20260315-0001 - Fix config.ts to read SCHEDULER_POLL_INTERVAL from environment

## Summary
Fixed `/workspace/extra/bread-baker/nanoclaw/src/config.ts` to properly read `SCHEDULER_POLL_INTERVAL` from environment variables.

## Changes Made

1. **Added `SCHEDULER_POLL_INTERVAL` to the `envConfig` read list** (line 12):
   - Now reads from .env file via `readEnvFile()`

2. **Updated `SCHEDULER_POLL_INTERVAL` export** (lines 21-26):
   - Now checks `process.env.SCHEDULER_POLL_INTERVAL` first
   - Falls back to `envConfig.SCHEDULER_POLL_INTERVAL` (from .env file)
   - Falls back to default value of `'15000'` (15000ms)

## Verification
- TypeScript compilation passes with no errors
- All acceptance criteria met:
  - 1. config.ts reads SCHEDULER_POLL_INTERVAL from process.env ✓
  - 2. Default value is 15000ms to match the .env file ✓
  - 3. No TypeScript compilation errors after the change ✓
